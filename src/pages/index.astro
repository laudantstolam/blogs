---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import BlogCard from '../components/BlogCard.astro';
import TypeText from '../components/TypeText.astro';
import 'bootstrap/dist/css/bootstrap.min.css';

import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import { getUrl } from '../utils/url';
import { imageConfig } from 'astro:assets';

const posts = (await getCollection('blog'))
    .filter(post => post.data.publish)
    .sort((a, b) => {
        const dateA = a.data.created_date?.valueOf() ?? 0;
        const dateB = b.data.created_date?.valueOf() ?? 0;
        return dateB - dateA;
    })
    .slice(0, 3);
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <Header />
    <div class="welcome">
      <div class="container">
        <div class="row align-items-center min-vh-100">
          <div class="col-lg-6 col-md-8">
            <h1>Ashley</h1>
            <p class="hero-description">
              I'm a <TypeText />
            </p>
          </div>
        </div>
      </div>
    </div>
    <main>
      <!-- About -->
      <section id="about" class="about">
        <div class="container" data-aos="fade-up">
          <div class="section-title">
            <h2>whoami</h2>
          </div>

          <div class="about-content">
            <div class="about-image-wrapper">
              <img
                src="https://raw.githubusercontent.com/Ash0645/image_remote/main/202401230105886.jpg"
                class="about-img"
                alt="Ashley's profile picture"
              />
              <div class="about-quick-facts">
                <div class="fact-item">
                  <i class="bi bi-geo-alt-fill"></i>
                  <span>Taipei, Taiwan</span>
                </div>
                <div class="fact-item">
                  <i class="bi bi-envelope-fill"></i>
                  <span>ashstolam@duck.com</span>
                </div>
              </div>
            </div>

            <div class="about-details">
              <div class="about-card">
                <div class="card-icon">
                  <i class="bi bi-person-badge"></i>
                </div>
                <div class="card-content">
                  <h3>Ashley Liu • 劉宜蓁</h3>
                  <p class="subtitle">20 years old • she/her</p>
                </div>
              </div>

              <div class="about-card">
                <div class="card-icon">
                  <i class="bi bi-mortarboard-fill"></i>
                </div>
                <div class="card-content">
                  <h4>National Taipei University of Technology</h4>
                  <p>Department of Intelligent Automation Engineering</p>
                  <a
                    href="https://iae.ntut.edu.tw/?Lang=en"
                    target="_blank"
                    class="learn-more">Learn more →</a
                  >
                </div>
              </div>

              <div class="about-card">
                <div class="card-icon">
                  <i class="bi bi-translate"></i>
                </div>
                <div class="card-content">
                  <h4>Languages</h4>
                  <div class="language-tags">
                    <span class="tag fluent">Mandarin</span>
                    <span class="tag fluent">English</span>
                    <span class="tag fluent">Japanese</span>
                    <span class="tag learning">German</span>
                    <span class="tag learning">Spanish</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Working Experiences Section -->
      <section id="experience" class="experience">
        <div class="container" data-aos="fade-up">
          <div class="section-title">
            <h2>Journey</h2>
            <p>Where I've been and what I've learned along the way</p>
          </div>

          <div class="timeline">
            <div class="timeline-item">
              <div class="timeline-marker"></div>
              <div class="timeline-content">
                <div class="timeline-header">
                  <img
                    alt="Gofreight-logo"
                    src="https://raw.githubusercontent.com/Ash0645/image_remote/main/20251109171902.png"
                    class="timeline-logo"
                  />
                  <div class="timeline-meta">
                    <h3>Data & AI Team Intern</h3>
                    <h4>Gofreight</h4>
                    <span class="timeline-date"
                      ><i class="bi bi-calendar3"></i> Mar 2025 - Aug 2025</span
                    >
                  </div>
                </div>
                <ul class="timeline-highlights">
                  <li>Develop SQL models based on Snowflake and dbt</li>
                  <li>
                    Maintain data ETL processes and CI/CD automated deployment,
                    managing data pipeline stability and version control through
                    GitHub Actions
                  </li>
                  <li>
                    Participate in internal meetings to understand
                    cross-departmental data requirements and user scenarios,
                    assisting in optimizing report and data workflow design
                  </li>
                </ul>
              </div>
            </div>

            <!-- <div class="timeline-item">
              <div class="timeline-marker cert"></div>
              <div class="timeline-content">
                <div class="timeline-header">
                  <img
                    alt="Certificate-logo"
                    src="https://raw.githubusercontent.com/Ash0645/image_remote/main/202402211608170.png"
                    class="timeline-logo cert"
                  />
                  <div class="timeline-meta">
                    <h3>MOEA Certified Information Security Engineer</h3>
                    <h4>Associate Level</h4>
                    <span class="timeline-date"
                      ><i class="bi bi-award-fill"></i> November 2023</span
                    >
                  </div>
                </div>
              </div>
            </div> -->

            <div class="timeline-item">
              <div class="timeline-marker"></div>
              <div class="timeline-content">
                <div class="timeline-header">
                  <img
                    alt="AUO-logo"
                    src="https://raw.githubusercontent.com/Ash0645/image_remote/main/20251109171711.png"
                    class="timeline-logo"
                  />
                  <div class="timeline-meta">
                    <h3>Software Development Intern</h3>
                    <h4>AU Optronics Corporation (AUO)</h4>
                    <span class="timeline-date"
                      ><i class="bi bi-calendar3"></i> Jul 2023 - Sep 2023</span
                    >
                  </div>
                </div>
                <ul class="timeline-highlights">
                  <li>
                    Using MVC architecture to developed a full-stack system to
                    optimize oven energy efficiency (30% electric saved)
                  </li>
                  <li>
                    Designed frontend interfaces and integrated APIs for
                    real-time monitoring/control
                  </li>
                  <li>
                    Collaborated with engineers to achieve measurable energy
                    reduction in production
                  </li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Skills Section -->
      <section id="skills" class="skills">
        <div class="container" data-aos="fade-up">
          <div class="section-title">
            <h2>Skills & Expertise</h2>
            <p>Exploring and advancing across multiple domains</p>
          </div>

          <div class="skills-grid">
            <div class="skill-category">
              <div class="category-header">
                <i class="bi bi-code-slash"></i>
                <h3>Software Development</h3>
              </div>
              <div class="skill-group">
                <h4>Languages</h4>
                <div class="skill-tags">
                  <span class="skill-tag">Python</span>
                  <span class="skill-tag">C/C++</span>
                  <span class="skill-tag">Go</span>
                  <span class="skill-tag">SQL</span>
                  <span class="skill-tag">Javascript</span>
                </div>
              </div>
              <div class="skill-group">
                <h4>Frameworks & Tools</h4>
                <div class="skill-tags">
                  <span class="skill-tag">Django</span>
                  <span class="skill-tag">React/NextJS</span>
                  <span class="skill-tag">Vue</span>
                  <span class="skill-tag">Flask</span>
                  <span class="skill-tag">FastAPI</span>
                </div>
              </div>
              <div class="skill-group">
                <h4>Database</h4>
                <div class="skill-tags">
                  <span class="skill-tag">MySQL/PostgreSQL</span>
                  <span class="skill-tag">MongoDB</span>
                  <span class="skill-tag">DynamoDB</span>
                </div>
              </div>
              <div class="skill-group">
                <h4>AI/ML</h4>
                <div class="skill-tags">
                  <span class="skill-tag">TensorFlow</span>
                  <span class="skill-tag">PyTorch</span>
                </div>
              </div>
            </div>

            <div class="skill-category">
              <div class="category-header">
                <i class="bi bi-shield-lock"></i>
                <h3>Cybersecurity</h3>
              </div>

			   <div class="skill-group">
                <h4>Focusing on reverse engineering & binary analysis</h4>
                <div class="skill-tags">
                  <span class="skill-tag">Static / Dynamic Analysis</span>
                  <span class="skill-tag">Firmware Emulation</span>
                  <span class="skill-tag">Windows Internal</span>
                </div>
              </div>
              <div class="skill-group">
                <h4>Achievements</h4>
                <div class="achievements">
                  <div class="achievement-badge">
                    <i class="bi bi-backpack-fill"></i>
                    <span>AIS3 2025</span>
                  </div>
                  <div class="achievement-badge">
                    <i class="bi bi-backpack-fill"></i>
                    <span>AIS3 Holyhigh Program 10th</span>
                  </div>
                  <div class="achievement-badge">
                    <i class="bi bi-backpack-fill"></i>
                    <span>TeamT5 Camp 2025</span>
                  </div>
                  <div class="achievement-badge">
                    <i class="bi bi-award"></i>
                    <span>AEGIS Pre: 4th • Final: 6th (2024)</span>
                  </div>
                  <div class="achievement-badge">
                    <i class="bi bi-award"></i>
                    <span>GICS Final: 1st (2025/2023) • 2nd (2024)</span>
                  </div>
                </div>
              </div>

             
            </div>

            <div class="skill-category">
              <div class="category-header">
                <i class="bi bi-gear"></i>
                <h3>Engineering</h3>
              </div>
              <div class="skill-group">
                <div class="skill-tags">
                  <span class="skill-tag">ROS System</span>
                  <span class="skill-tag">AutoCAD</span>
                  <span class="skill-tag">EDM Machine</span>
                </div>
              </div>
            </div>

            <div class="skill-category">
              <div class="category-header">
                <i class="bi bi-palette"></i>
                <h3>Design & Visualization</h3>
              </div>
              <div class="skill-group">
                <h4>JS Libraries</h4>
                <div class="skill-tags">
                  <span class="skill-tag">D3.js</span>
                  <span class="skill-tag">ECharts</span>
                  <span class="skill-tag">ApexChart</span>
                  <span class="skill-tag">Mapbox</span>
                  <span class="skill-tag">P5.js</span>
                  <span class="skill-tag">Three.js</span>
                </div>
              </div>
              <div class="skill-group">
                <h4>Design Tools</h4>
                <div class="skill-tags">
                  <span class="skill-tag">Photoshop</span>
                  <span class="skill-tag">Illustrator</span>
                  <span class="skill-tag">InDesign</span>
                  <span class="skill-tag">Figma</span>
                  <span class="skill-tag">Canva</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Latest Blog Posts -->
      <section class="latest-posts">
        <div class="section-title">
          <h2>Latest Blog Posts</h2>
        </div>
        <div class="post-grid">
          {
            posts.map((post) => (
              <BlogCard
                title={post.data.title}
                description={post.data.description ?? undefined}
                date={post.data.created_date ?? undefined}
                slug={post.id}
              />
            ))
          }
        </div>
        <div class="view-all">
          <a href={getUrl("/blog")} class="secondary-button">View All Posts</a>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>

<style>
  /* Override global accent to white for index page only */
  :root {
    --accent: #ffffff;
  }

  :root {
    /* Tech color palette - light mode */
    --terminal-bg: #ffffff;
    --terminal-surface: #bdd8e9;
    --terminal-text: #0a4174;
    --terminal-accent: #4e8ea2;
    --terminal-highlight: #7bbde8;
    --terminal-muted: #49769f;
    --terminal-border: #6ea2b3;

    /* Neumorphism shadows for light mode */
    --shadow-1: 6px 6px 12px rgba(0, 29, 57, 0.15);
    --shadow-2: -6px -6px 12px rgba(255, 255, 255, 0.8);
    --shadow-inset-1: inset 3px 3px 6px rgba(0, 29, 57, 0.1);
    --shadow-inset-2: inset -3px -3px 6px rgba(255, 255, 255, 0.6);
  }

  [data-theme="dark"] {
    /* Tech color palette - dark mode */
    --terminal-bg: #333d4d;
    --terminal-surface: #0a4174;
    --terminal-text: #bdd8e9;
    --terminal-accent: #7bbde8;
    --terminal-highlight: #6ea2b3;
    --terminal-muted: #49769f;
    --terminal-border: #4e8ea2;

    /* Neumorphism shadows for dark mode */
    --shadow-1: 6px 6px 12px rgba(0, 0, 0, 0.5);
    --shadow-2: -6px -6px 12px rgba(73, 118, 159, 0.08);
    --shadow-inset-1: inset 3px 3px 6px rgba(0, 0, 0, 0.4);
    --shadow-inset-2: inset -3px -3px 6px rgba(73, 118, 159, 0.05);
  }

  body {
    background-color: var(--terminal-bg);
    background-image: url("https://raw.githubusercontent.com/Ash0645/image_remote/main/ash-bg.png");
    background-position: top right;
    background-repeat: no-repeat;
    background-size: contain;
    transition: background-color 0.3s ease;
    position: relative;
    overflow-x: hidden;
  }

  body::before {
    content: "";
    position: fixed;
    top: -20%;
    left: -10%;
    width: 40%;
    height: 40%;
    background: radial-gradient(
      circle,
      var(--terminal-accent) 0%,
      transparent 70%
    );
    opacity: 0.03;
    border-radius: 50%;
    pointer-events: none;
    z-index: 0;
    animation: float 20s ease-in-out infinite;
  }

  @keyframes float {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    50% {
      transform: translate(50px, 30px) scale(1.1);
    }
  }

  main {
    max-width: 100%;
    padding: 0;
    position: relative;
    z-index: 1;
  }

  .welcome {
    position: relative;
    display: flex;
    align-items: center;
    padding: 4rem 0;
  }

  .welcome h1 {
    color: #ffffff;
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    font-family: var(--font-mono);
    letter-spacing: -0.02em;
    position: relative;
  }

  .welcome h1::before {
    content: "> ";
    color: #ffffff;
    opacity: 0.6;
  }

  .social-links {
    margin-top: 2rem;
  }

  .social-links a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    border-radius: 12px;
    background: var(--terminal-bg);
    color: var(--terminal-text);
    margin-right: 15px;
    transition: all 0.3s ease;
    text-decoration: none;
    box-shadow: var(--shadow-1), var(--shadow-2);
    border: 1px solid var(--terminal-border);
    border-opacity: 0.1;
  }

  .social-links a:hover {
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    color: var(--terminal-accent);
    transform: translateY(2px);
  }

  .hero {
    padding: 8rem 1rem;
    text-align: left;
    background: transparent;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: "";
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(
      circle at center,
      rgba(var(--accent), 0.08) 0%,
      rgba(var(--accent), 0.03) 25%,
      transparent 50%
    );
    z-index: -1;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
    position: relative;
  }

  .hero-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(var(--accent), 0.1);
    color: var(--accent);
    border-radius: 2rem;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 2rem;
    transition: var(--theme-transition);
  }

  .hero h1 {
    font-size: 4rem;
    margin-bottom: 1.5rem;
    line-height: 1.1;
    color: rgb(var(--black));
    font-family: var(--font-heading);
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .spacer-lg {
    height: 30rem;
  }

  .gradient-text {
    background: linear-gradient(
      135deg,
      var(--accent) 0%,
      var(--accent-dark) 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    color: transparent;
  }

  .hero-description {
    font-size: 1.25rem;
    margin-bottom: 2.5rem;
    color: #ffffff;
    max-width: 600px;
    line-height: 1.6;
    font-family: var(--font-mono);
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    align-items: center;
  }

  .primary-button,
  .secondary-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-family: var(--font-mono);
    transition: all 0.3s ease;
    border: 2px solid var(--terminal-border);
    position: relative;
    overflow: hidden;
  }

  .primary-button::before,
  .secondary-button::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      var(--terminal-accent),
      transparent
    );
    opacity: 0.3;
    transition: left 0.5s;
  }

  .primary-button:hover::before,
  .secondary-button:hover::before {
    left: 100%;
  }

  .primary-button {
    background: var(--terminal-bg);
    color: var(--terminal-text);
    box-shadow: var(--shadow-1), var(--shadow-2);
  }

  .primary-button:hover {
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    transform: translateY(2px);
    text-decoration: none;
  }

  .primary-button svg {
    transition: transform 0.3s ease;
  }

  .primary-button:hover svg {
    transform: translateX(4px);
  }

  .secondary-button {
    background: var(--terminal-bg);
    color: var(--terminal-muted);
    box-shadow: var(--shadow-1), var(--shadow-2);
  }

  .secondary-button:hover {
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    transform: translateY(2px);
    color: var(--terminal-accent);
    text-decoration: none;
  }

  .secondary-button svg {
    opacity: 0.8;
  }

  .latest-posts {
    padding: 4rem 1rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .post-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .view-all {
    text-align: center;
    margin-top: 2rem;
  }

  .about {
    padding: 4rem 0;
    background: transparent;
  }

  .experience,
  .skills {
    padding: 4rem 0;
    background: transparent;
    position: relative;
  }

  /* About Section - Modern Card Layout */
  .about-content {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 3rem;
    margin-top: 3rem;
  }

  .about-image-wrapper {
    position: relative;
  }

  .about-img {
    width: 100%;
    border-radius: 12px;
    transition: all 0.3s ease;
    box-shadow: var(--shadow-1), var(--shadow-2);
    border: 2px solid var(--terminal-border);
    border-opacity: 0.2;
  }

  .about-img:hover {
    transform: scale(1.02);
    box-shadow:
      8px 8px 16px var(--terminal-accent),
      -8px -8px 16px rgba(255, 255, 255, 0.1);
  }

  .about-quick-facts {
    margin-top: 1.5rem;
    background: var(--terminal-bg);
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: var(--shadow-1), var(--shadow-2);
    transition: all 0.3s ease;
    border: 1px solid var(--terminal-border);
    border-opacity: 0.3;
    position: relative;
  }

  .about-quick-facts::before {
    content: "# INFO";
    position: absolute;
    top: -10px;
    left: 15px;
    background: var(--terminal-bg);
    padding: 0 8px;
    font-size: 0.75rem;
    color: var(--terminal-accent);
    font-family: var(--font-mono);
    font-weight: 700;
  }

  .fact-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.5rem 0;
    color: var(--terminal-text);
    font-family: var(--font-mono);
    font-size: 0.9rem;
  }

  .fact-item i {
    color: var(--terminal-accent);
    font-size: 1.2rem;
  }

  .about-details {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .about-card {
    background: var(--terminal-bg);
    border-radius: 12px;
    padding: 2rem;
    box-shadow: var(--shadow-1), var(--shadow-2);
    display: flex;
    gap: 1.5rem;
    transition: all 0.3s ease;
    border: 1px solid var(--terminal-border);
    border-opacity: 0.2;
    position: relative;
    overflow: hidden;
  }

  .about-card::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0;
    width: 4px;
    height: 0;
    background: var(--terminal-accent);
    transition: height 0.3s ease;
  }

  .about-card:hover::before {
    height: 100%;
  }

  .about-card:hover {
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    transform: translateX(4px);
    border-color: var(--terminal-accent);
  }

  .card-icon {
    width: 50px;
    height: 50px;
    background: var(--terminal-bg);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    border: 1px solid var(--terminal-border);
  }

  .card-icon i {
    font-size: 1.5rem;
    color: var(--terminal-accent);
  }

  .card-content h3 {
    font-size: 1.5rem;
    color: var(--terminal-text);
    margin-bottom: 0.5rem;
    font-family: var(--font-mono);
  }

  .card-content h4 {
    font-size: 1.2rem;
    color: var(--terminal-muted);
    margin-bottom: 0.5rem;
    font-weight: 600;
  }

  .card-content p {
    color: var(--terminal-muted);
    line-height: 1.6;
    margin: 0.5rem 0;
  }

  .card-content .subtitle {
    color: var(--terminal-muted);
    font-size: 1rem;
    font-family: var(--font-mono);
  }

  .learn-more {
    display: inline-flex;
    align-items: center;
    color: var(--terminal-accent);
    text-decoration: none;
    margin-top: 0.5rem;
    font-weight: 500;
    font-family: var(--font-mono);
    transition: all 0.2s ease;
  }

  .learn-more::after {
    content: " →";
    transition: transform 0.2s ease;
    display: inline-block;
  }

  .learn-more:hover::after {
    transform: translateX(4px);
  }

  .learn-more:hover {
    color: var(--terminal-highlight);
  }

  .language-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 0.75rem;
  }

  .language-tags .tag {
    padding: 0.4rem 0.9rem;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 600;
    font-family: var(--font-mono);
    transition: all 0.3s ease;
    border: 1px solid var(--terminal-border);
    position: relative;
  }

  .language-tags .tag.fluent {
    background: var(--terminal-bg);
    color: var(--terminal-text);
    box-shadow: var(--shadow-1), var(--shadow-2);
  }

  .language-tags .tag.fluent::before {
    content: "●";
    color: var(--terminal-accent);
    margin-right: 0.4rem;
  }

  .language-tags .tag.learning {
    background: var(--terminal-bg);
    color: var(--terminal-muted);
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
  }

  .language-tags .tag.learning::before {
    content: "○";
    color: var(--terminal-accent);
    margin-right: 0.4rem;
  }

  .language-tags .tag:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-1), var(--shadow-2);
    border-color: var(--terminal-accent);
    color: var(--terminal-accent);
  }

  /* Timeline Section - Terminal Style */
  .timeline {
    position: relative;
    max-width: 900px;
    margin: 3rem auto;
    padding-left: 50px;
  }

  .timeline::before {
    content: "";
    position: absolute;
    left: 20px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--terminal-border);
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
  }

  .timeline-item {
    position: relative;
    margin-bottom: 3rem;
  }

  .timeline-marker {
    position: absolute;
    left: -38px;
    top: 0;
    width: 16px;
    height: 16px;
    background: var(--terminal-accent);
    border-radius: 2px;
    z-index: 2;
    box-shadow: 0 0 8px var(--terminal-accent);
    transition: all 0.3s ease;
    transform: rotate(45deg);
  }

  .timeline-marker.cert {
    background: var(--terminal-highlight);
    box-shadow: 0 0 12px var(--terminal-highlight);
  }

  .timeline-content {
    background: var(--terminal-bg);
    border-radius: 8px;
    padding: 2rem;
    box-shadow: var(--shadow-1), var(--shadow-2);
    margin-left: 30px;
    transition: all 0.3s ease;
    border: 1px solid var(--terminal-border);
    border-opacity: 0.2;
    position: relative;
  }

  .timeline-content::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--terminal-accent), transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .timeline-content:hover::before {
    opacity: 1;
  }

  .timeline-content:hover {
    transform: translateX(8px);
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    border-color: var(--terminal-accent);
  }

  .timeline-header {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .timeline-logo {
    width: 80px;
    height: 80px;
    object-fit: contain;
    background: var(--terminal-bg);
    border-radius: 8px;
    padding: 10px;
    flex-shrink: 0;
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    border: 1px solid var(--terminal-border);
  }

  .timeline-meta h3 {
    font-size: 1.3rem;
    color: var(--terminal-text);
    margin-bottom: 0.25rem;
    font-weight: 700;
    font-family: var(--font-mono);
  }

  .timeline-meta h4 {
    font-size: 1rem;
    color: var(--terminal-accent);
    margin-bottom: 0.75rem;
    font-weight: 500;
  }

  .timeline-date {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--terminal-muted);
    font-size: 0.9rem;
    font-family: var(--font-mono);
  }

  .timeline-date i {
    color: var(--terminal-accent);
  }

  .timeline-highlights {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .timeline-highlights li {
    padding: 0.5rem 0 0.5rem 1.5rem;
    position: relative;
    color: var(--terminal-muted);
    line-height: 1.6;
  }

  .timeline-highlights li::before {
    content: "$ ";
    position: absolute;
    left: 0;
    color: var(--terminal-accent);
    font-weight: bold;
    font-family: var(--font-mono);
  }

  .section-title {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-title h2 {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--terminal-text);
    margin-bottom: 1.5rem;
    position: relative;
    display: inline-block;
    padding: 0.5rem 2rem;
    background: var(--terminal-bg);
    border-radius: 8px;
    box-shadow: var(--shadow-1), var(--shadow-2);
    font-family: var(--font-mono);
    border: 2px solid var(--terminal-border);
  }

  .section-title h2::before {
    content: "# ";
    color: var(--terminal-accent);
  }

  .section-title p {
    color: var(--terminal-muted);
    font-size: 1.05rem;
    max-width: 800px;
    margin: 0 auto;
    font-family: var(--font-mono);
  }

  /* Skills Section - Terminal Style */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .skill-category {
    background: var(--terminal-bg);
    border-radius: 8px;
    padding: 2rem;
    box-shadow: var(--shadow-1), var(--shadow-2);
    transition: all 0.3s ease;
    border: 1px solid var(--terminal-border);
    border-opacity: 0.3;
    position: relative;
    overflow: hidden;
  }

  .skill-category::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 100%;
    height: 3px;
    background: var(--terminal-accent);
    transition: right 0.3s ease;
  }

  .skill-category:hover::before {
    right: 0;
  }

  .skill-category:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    border-color: var(--terminal-accent);
  }

  .category-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--terminal-border);
    border-opacity: 0.2;
  }

  .category-header i {
    font-size: 1.5rem;
    color: var(--terminal-accent);
    background: var(--terminal-bg);
    width: 45px;
    height: 45px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 6px;
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    border: 1px solid var(--terminal-border);
  }

  .category-header h3 {
    font-size: 1.3rem;
    color: var(--terminal-text);
    margin: 0;
    font-weight: 700;
    font-family: var(--font-mono);
  }

  .skill-group {
    margin-bottom: 1.5rem;
  }

  .skill-group:last-child {
    margin-bottom: 0;
  }

  .skill-group h4 {
    font-size: 0.75rem;
    color: var(--terminal-muted);
    margin-bottom: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-family: var(--font-mono);
  }

  .skill-description {
    font-size: 0.9rem;
    color: var(--terminal-muted);
    margin-bottom: 1rem;
    line-height: 1.5;
  }

  .skill-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .skill-tag {
    display: inline-block;
    padding: 0.4rem 0.9rem;
    background: var(--terminal-bg);
    color: var(--terminal-text);
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 600;
    font-family: var(--font-mono);
    transition: all 0.3s ease;
    box-shadow: var(--shadow-1), var(--shadow-2);
    border: 1px solid var(--terminal-border);
  }

  .skill-tag:hover {
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    color: var(--terminal-accent);
    transform: translateY(2px);
    border-color: var(--terminal-accent);
  }

  .achievements {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-top: 1rem;
  }

  .achievement-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--terminal-bg);
    padding: 0.5rem 1rem;
    border-radius: 6px;
    color: var(--terminal-text);
    font-size: 0.85rem;
    width: fit-content;
    box-shadow: var(--shadow-1), var(--shadow-2);
    transition: all 0.3s ease;
    border: 1px solid var(--terminal-accent);
    font-family: var(--font-mono);
  }

  .achievement-badge:hover {
    box-shadow: var(--shadow-inset-1), var(--shadow-inset-2);
    transform: translateY(2px);
  }

  .achievement-badge i {
    color: var(--terminal-accent);
    font-size: 1.1rem;
  }

  /* Latest posts section theming */
  .latest-posts h2 {
    color: var(--terminal-text);
    text-align: center;
    margin-bottom: 2rem;
  }

  :global(.latest-posts .blog-card:hover .title) {
    color: var(--terminal-accent) !important;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 6rem 1rem;
    }

    .hero h1 {
      font-size: 2.5rem;
    }

    .hero-description {
      font-size: 1.125rem;
      padding: 0 1rem;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: stretch;
      padding: 0 2rem;
    }

    .primary-button,
    .secondary-button {
      justify-content: center;
    }

    .latest-posts {
      padding: 4rem 1rem;
    }

    .post-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .view-all {
      margin-top: 1.5rem;
    }

    /* Mobile adjustments for new sections */
    .welcome h1 {
      font-size: 2.5rem;
    }

    .welcome {
      padding: 2rem 1rem;
    }

    .about,
    .experience,
    .skills {
      padding: 3rem 0;
    }

    .section-title h2 {
      font-size: 2rem;
    }

    /* About section mobile */
    .about-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .about-card {
      padding: 1.5rem;
    }

    .about-card:hover {
      transform: translateY(-4px);
    }

    .card-icon {
      width: 40px;
      height: 40px;
    }

    .card-content h3 {
      font-size: 1.3rem;
    }

    .card-content h4 {
      font-size: 1.1rem;
    }

    /* Timeline mobile */
    .timeline {
      padding-left: 30px;
    }

    .timeline::before {
      left: 10px;
    }

    .timeline-marker {
      left: -28px;
      width: 14px;
      height: 14px;
      border-width: 3px;
    }

    .timeline-content {
      padding: 1.5rem;
      margin-left: 20px;
    }

    .timeline-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .timeline-logo {
      width: 60px;
      height: 60px;
    }

    .timeline-meta h3 {
      font-size: 1.2rem;
    }

    .timeline-meta h4 {
      font-size: 1rem;
    }

    /* Skills mobile */
    .skills-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .skill-category {
      padding: 1.5rem;
    }

    .skill-category:hover {
      transform: translateY(-4px);
    }

    .category-header {
      gap: 0.75rem;
    }

    .category-header i {
      width: 40px;
      height: 40px;
      font-size: 1.5rem;
    }

    .category-header h3 {
      font-size: 1.2rem;
    }

    .skill-tag {
      font-size: 0.85rem;
      padding: 0.4rem 0.8rem;
    }

    .achievement-badge {
      font-size: 0.85rem;
      padding: 0.4rem 0.8rem;
    }
  }
</style>
